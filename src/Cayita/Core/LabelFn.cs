using System;
using System.Runtime.CompilerServices;

namespace Cayita
{
	/*
	[PreserveMemberCase]
	[IgnoreNamespace]
	public static class LabelFn
	{
		public static void SetFor(Label atom, string value)
		{
			atom.El.SetAttribute ("for", value);
		}

		public static string GetFor(Label atom)
		{
			var o =atom.El.GetAttribute ("for");
			return o==null? "": o.ToString();
		}

	}
	*/
}

